# fabric/

Fabric workspace items synced via Fabric Git Integration.

## Dataflows (Phase 1 — Bronze ingestion)

| Folder | What it does |
|--------|-------------|
| `df_ecb_fx.Dataflow/` | ECB CSV API → `bronze_fx_rates` |
| `df_worldbank_gdp.Dataflow/` | World Bank JSON API → `bronze_gdp` |
| `df_openaq.Dataflow/` | OpenAQ JSON API (paginated) → `bronze_air_quality` |

## Pipelines (Phase 1 — Bronze ingestion)

| Folder | What it does |
|--------|-------------|
| `pl_ingest_nyc_taxi.DataPipeline/` | HTTP → Parquet → `bronze_lakehouse/Files/raw/taxi/` |

## Auto-generated by Fabric Git export

`bronze_lakehouse.Lakehouse/`, `silver_lakehouse.Lakehouse/`, `gold_warehouse.Warehouse/`
— exported automatically by Fabric for all workspace items.
These resources are actually managed by Terraform (`terraform/`), not by these files.
